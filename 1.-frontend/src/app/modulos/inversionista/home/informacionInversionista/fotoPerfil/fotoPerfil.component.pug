mat-card.foto-perfil-container
    .imagen-container
        .imagen(*ngIf="!fotoAvatar")
            img([src]="fotoPerfil")
        .imagen(*ngIf="fotoAvatar")
            img([src]="fotoAvatar")
        h4 Foto de Perfil
